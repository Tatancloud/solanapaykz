# Handoff

## State
Этап 1 из 4 (Core SDK). Спецификация `docs/superpowers/specs/2026-09-07-core-sdk-design.md` УТВЕРЖДЕНА 7 сентября 2026. ТЗ оформлено в `docs/TZ.md` (раздел 10 — уточнения), задачи в `docs/TODO.md`. Всё в `main`, коммит 2aadf5e запушен.
Согласовано: Binance USDTKZT основной источник курса, синтетика резервная; `@solana/pay@1.0.26` + `@solana/kit@6.10`; котировка живёт 15 минут; наценка по умолчанию 0.

## Next
1. Написать план реализации по утверждённой спеке.
2. Каркас пакета (TS strict, vitest, tsup), затем `rates/` по TDD — первыми тесты фолбэка и молчаливого отказа CoinGecko.
3. Этапы 2-4 (WooCommerce, Tilda, демо) не трогать, пока этап 1 не закрыт.

## Context
- CoinGecko НЕ поддерживает KZT: отвечает HTTP 200 и `{"usd-coin":{}}`. Пустой ответ обязан считаться отказом источника, иначе сумма к оплате станет нулевой. ТЗ в этом месте ошибается.
- `@solana/kit@8` ставить нельзя — ERESOLVE с `@solana/pay` (peer `^6.4.0`).
- Push только по SSH (`git@github.com:...`), HTTPS не аутентифицируется. Ключ уже привязан как deploy key.
- Домена у проекта нет, nginx намеренно не настраивался. Демо-магазин планируется на GitHub Pages.
- Руслан пишет и читает по-русски; отвечать ему в Telegram через `reply` — транскрипт до него не доходит.
